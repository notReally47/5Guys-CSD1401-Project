Run generateLevelCSV.exe to in cmd or wsl.

The program will ask for different inputs. Please read and follow the input format correctly.

The program will then generate the level csv file.

If generateLevelCSSV.c was edited, compile into an executable file in wsl with:
gcc -o generateLevelCSV.exe generateLevelCSV.c
